/**
 * Pagination offset interface
 */
export interface PaginationOffset {
  /**
   * Offset number
   */
  count: number;
  /**
   * Pages n.o.
   */
  num_pages: number;
  /**
   * Items per page
   */
  per_page: number;
}
